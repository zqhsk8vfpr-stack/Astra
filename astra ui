loadstring([[
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/i77lhm/vaderpaste/refs/heads/main/library.lua"))()
if not library then error("UI library failed to load") end
local flags = library.flags

local window = library:window({
    name = "Astra",
    size = UDim2.fromOffset(500, 350),
})

local combat = window:tab({ name = "Combat" })
local visuals = window:tab({ name = "Visuals" })
local misc = window:tab({ name = "Misc" })
local weapons = window:tab({ name = "Weapons" })
local settings = window:tab({ name = "Settings" })

local aimbotSection = combat:section({ name = "Aimbot" })

aimbotSection:toggle({
    name = "Enable",
    flag = "combat_enable",
    default = false,
    tooltip = "Master switch for aim assist",
    callback = function(val) getgenv().Enabled = val end,
})

aimbotSection:keybind({
    name = "Key",
    flag = "combat_key",
    default = Enum.KeyCode.E,
    tooltip = "Key to lock on",
    callback = function(newKey)
        if newKey and type(newKey) == "userdata" and typeof(newKey) == "EnumItem" then
            getgenv().AimbotKey = newKey.Name
        else
            getgenv().AimbotKey = "E"
        end
    end,
})

aimbotSection:dropdown({
    name = "Activation",
    flag = "combat_activation",
    items = { "Hold", "Toggle", "Rage", "Mobile" },
    multi = false,
    default = "Hold",
    tooltip = "Activation mode: Hold = key held, Toggle = key press toggles, Rage = Locks on everyone, Mobile = touch based",
    callback = function(val) getgenv().Activation = val end,
})

aimbotSection:toggle({
    name = "Smoothness",
    flag = "combat_smoothness",
    default = false,
    tooltip = "Enable disable smoothness",
    callback = function(val) getgenv().SmoothnessEnabled = val end,
})

aimbotSection:textbox({
    name = "Smoothness X",
    flag = "combat_smoothness_x",
    default = "",
    placeholder = "Smoothness X",
    tooltip = "Horizontal smoothness",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SmoothnessX = num else getgenv().SmoothnessX = 0.1 end
    end,
})

aimbotSection:textbox({
    name = "Smoothness Y",
    flag = "combat_smoothness_y",
    default = "",
    placeholder = "Smoothness Y",
    tooltip = "Vertical smoothness",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SmoothnessY = num else getgenv().SmoothnessY = 0.1 end
    end,
})

aimbotSection:dropdown({
    name = "Easing Style",
    flag = "combat_easing_style",
    items = { "Exponential", "Linear", "Bounce", "Sine", "Cubic", "Elastic", "Quad", "Quart", "Back" },
    multi = false,
    default = "Linear",
    tooltip = "Smoothing curve for aim assist",
    callback = function(val) getgenv().EasingStyle = val end,
})

aimbotSection:dropdown({
    name = "Easing Direction",
    flag = "combat_easing_direction",
    items = { "In", "Out", "Inout" },
    multi = false,
    default = "Out",
    tooltip = "Direction of the easing curve",
    callback = function(val) getgenv().EasingDirection = val end,
})

aimbotSection:textbox({
    name = "Jump Offset",
    flag = "combat_jump_offset",
    default = "",
    placeholder = "Jump Offset",
    tooltip = "Vertical offset when target is jumping",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().JumpOffset = num else getgenv().JumpOffset = 0 end
    end,
})

aimbotSection:textbox({
    name = "Fall Offset",
    flag = "combat_fall_offset",
    default = "",
    placeholder = "Fall Offset",
    tooltip = "Vertical offset when target is falling",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().FallOffset = num else getgenv().FallOffset = 0 end
    end,
})

aimbotSection:toggle({
    name = "Prediction",
    flag = "combat_prediction",
    default = false,
    tooltip = "Enable disable prediction",
    callback = function(val) getgenv().Prediction = val end,
})

aimbotSection:textbox({
    name = "Prediction X",
    flag = "combat_prediction_x",
    default = "",
    placeholder = "Prediction X",
    tooltip = "Horizontal prediction multiplier",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().PredictionX = num else getgenv().PredictionX = 0.115 end
    end,
})

aimbotSection:textbox({
    name = "Prediction Y",
    flag = "combat_prediction_y",
    default = "",
    placeholder = "Prediction Y",
    tooltip = "Vertical prediction multiplier",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().PredictionY = num else getgenv().PredictionY = 0.115 end
    end,
})

aimbotSection:textbox({
    name = "Prediction Z",
    flag = "combat_prediction_z",
    default = "",
    placeholder = "Prediction Z",
    tooltip = "Depth prediction multiplier",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().PredictionZ = num else getgenv().PredictionZ = 0.115 end
    end,
})

aimbotSection:toggle({
    name = "Auto Prediction",
    flag = "combat_auto_prediction",
    default = false,
    tooltip = "Enable disable auto prediction",
    callback = function(val) getgenv().AutoPrediction = val end,
})

aimbotSection:dropdown({
    name = "Auto Prediction Mode",
    flag = "combat_auto_prediction_mode",
    items = { "Regular", "Ping based", "Advanced" },
    multi = false,
    default = "Regular",
    tooltip = "Select auto prediction mode",
    callback = function(val) getgenv().AutoPredictionMode = val end,
})

aimbotSection:toggle({
    name = "Target Outline",
    flag = "combat_target_outline",
    default = false,
    tooltip = "Camlock targets closest outline of enemy",
    callback = function(val) getgenv().TargetOutline = val end,
})

aimbotSection:dropdown({
    name = "Hitpart Selection",
    flag = "combat_hitpart_selection",
    items = { "Direct", "Closest", "Nearest" },
    multi = false,
    default = "Direct",
    tooltip = "How the aimbot selects which part to aim at: Direct = fixed part, Closest = closest to crosshair, Nearest = nearest in 3D space",
    callback = function(val) getgenv().HitpartSelection = val end,
})

aimbotSection:dropdown({
    name = "Target Priority",
    flag = "combat_target_priority",
    items = { "Closest", "Enemy look direction" },
    multi = false,
    default = "Closest",
    tooltip = "How to select which enemy to target: Closest = nearest to you, Enemy look direction = prioritize enemies looking at you",
    callback = function(val) getgenv().TargetPriority = val end,
})

aimbotSection:toggle({
    name = "Wall Check",
    flag = "combat_aimbot_wall_check",
    default = false,
    tooltip = "Require line of sight to target",
    callback = function(val) getgenv().AimbotWallCheck = val end,
})

aimbotSection:toggle({
    name = "Knocked Check",
    flag = "combat_aimbot_knocked_check",
    default = false,
    tooltip = "Ignore knocked players",
    callback = function(val) getgenv().AimbotKnockCheck = val end,
})

aimbotSection:toggle({
    name = "Friend Check",
    flag = "combat_aimbot_friend_check",
    default = false,
    tooltip = "Ignore friends",
    callback = function(val) getgenv().AimbotFriendCheck = val end,
})

aimbotSection:toggle({
    name = "Forcefield Check",
    flag = "combat_aimbot_forcefield_check",
    default = false,
    tooltip = "Ignore enemies with forcefield",
    callback = function(val) getgenv().AimbotForcefieldCheck = val end,
})

local triggerSection = combat:section({ name = "Triggerbot", side = "right" })

triggerSection:toggle({
    name = "Enable",
    flag = "combat_triggerbot_enable",
    default = false,
    tooltip = "Enable triggerbot auto shoot when crosshair is over enemy",
    callback = function(val) getgenv().TriggerbotEnabled = val end,
})

triggerSection:keybind({
    name = "Key",
    flag = "combat_triggerbot_key",
    default = Enum.KeyCode.F,
    tooltip = "Key to activate triggerbot",
    callback = function(newKey)
        if newKey and type(newKey) == "userdata" and typeof(newKey) == "EnumItem" then
            getgenv().TriggerbotKey = newKey.Name
        else
            getgenv().TriggerbotKey = "F"
        end
    end,
})

triggerSection:dropdown({
    name = "Mode",
    flag = "combat_triggerbot_mode",
    items = { "Fov", "Silent", "Hitbox" },
    multi = false,
    default = "Fov",
    tooltip = "Triggerbot mode: Fov = uses FOV circle, Silent = redirects bullets without moving camera, Hitbox = aims at specific hitbox",
    callback = function(val) getgenv().TriggerbotMode = val end,
})

triggerSection:textbox({
    name = "Delay",
    flag = "combat_triggerbot_delay",
    default = "",
    placeholder = "Trigger Delay",
    tooltip = "Reaction time in milliseconds",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().TriggerbotDelay = num else getgenv().TriggerbotDelay = 120 end
    end,
})

triggerSection:toggle({
    name = "Prediction",
    flag = "combat_triggerbot_prediction",
    default = false,
    tooltip = "Enable prediction for triggerbot",
    callback = function(val) getgenv().TriggerbotPrediction = val end,
})

triggerSection:textbox({
    name = "Prediction X",
    flag = "combat_triggerbot_prediction_x",
    default = "",
    placeholder = "Prediction X",
    tooltip = "Horizontal prediction multiplier for triggerbot",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().TriggerbotPredX = num else getgenv().TriggerbotPredX = 0.115 end
    end,
})

triggerSection:textbox({
    name = "Prediction Y",
    flag = "combat_triggerbot_prediction_y",
    default = "",
    placeholder = "Prediction Y",
    tooltip = "Vertical prediction multiplier for triggerbot",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().TriggerbotPredY = num else getgenv().TriggerbotPredY = 0.115 end
    end,
})

triggerSection:toggle({
    name = "Knocked Check",
    flag = "combat_knocked_check",
    default = false,
    tooltip = "Ignore knocked players",
    callback = function(val) getgenv().KnockCheck = val end,
})

triggerSection:toggle({
    name = "Wall Check",
    flag = "combat_wall_check",
    default = false,
    tooltip = "Require line of sight to target",
    callback = function(val) getgenv().WallCheck = val end,
})

triggerSection:toggle({
    name = "Tool Check",
    flag = "combat_tool_check",
    default = false,
    tooltip = "Only trigger when holding a weapon",
    callback = function(val) getgenv().ToolCheck = val end,
})

triggerSection:toggle({
    name = "Friend Check",
    flag = "combat_friend_check",
    default = false,
    tooltip = "Ignore friends",
    callback = function(val) getgenv().FriendCheck = val end,
})

triggerSection:toggle({
    name = "Forcefield Check",
    flag = "combat_forcefield_check",
    default = false,
    tooltip = "Ignore enemies with forcefield",
    callback = function(val) getgenv().ForcefieldCheck = val end,
})

local silentSection = combat:section({ name = "Silent Aim", side = "right" })

silentSection:toggle({
    name = "Enable",
    flag = "combat_silent_enable",
    default = false,
    tooltip = "Enable silent aim redirects bullets without moving your camera",
    callback = function(val) getgenv().SilentAimEnabled = val end,
})

silentSection:keybind({
    name = "Key",
    flag = "combat_silent_key",
    default = Enum.KeyCode.G,
    tooltip = "Key to toggle silent aim on off",
    callback = function(newKey)
        if newKey and type(newKey) == "userdata" and typeof(newKey) == "EnumItem" then
            getgenv().SilentAimKey = newKey.Name
        else
            getgenv().SilentAimKey = "G"
        end
    end,
})

silentSection:dropdown({
    name = "Aim Method",
    flag = "combat_silent_method",
    items = { "Direct", "Closest Part", "Nearest Point" },
    multi = false,
    default = "Direct",
    tooltip = "How to select the aim point: Direct = fixed hitbox, Closest Part = nearest to crosshair, Nearest Point = nearest in 3D",
    callback = function(val) getgenv().SilentAimMethod = val end,
})

silentSection:textbox({
    name = "Hit Chance",
    flag = "combat_silent_hitchance",
    default = "",
    placeholder = "Silent aim Hitchance",
    tooltip = "Chance to hit the selected target",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilentAimHitChance = num else getgenv().SilentAimHitChance = 85 end
    end,
})

silentSection:toggle({
    name = "Anti Aimviewer",
    flag = "combat_silent_anti_aimview",
    default = false,
    tooltip = "Prevents the aimbot from adjusting your camera view (keep your aim still)",
    callback = function(val) getgenv().SilentAimAntiAimView = val end,
})

silentSection:toggle({
    name = "Prediction",
    flag = "combat_silent_prediction",
    default = false,
    tooltip = "Enable prediction for silent aim",
    callback = function(val) getgenv().SilentAimPrediction = val end,
})

silentSection:textbox({
    name = "Prediction X",
    flag = "combat_silent_prediction_x",
    default = "",
    placeholder = "Prediction X",
    tooltip = "Horizontal prediction multiplier for silent aim",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilentAimPredX = num else getgenv().SilentAimPredX = 0.115 end
    end,
})

silentSection:textbox({
    name = "Prediction Y",
    flag = "combat_silent_prediction_y",
    default = "",
    placeholder = "Prediction Y",
    tooltip = "Vertical prediction multiplier for silent aim",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilentAimPredY = num else getgenv().SilentAimPredY = 0.115 end
    end,
})

silentSection:textbox({
    name = "Prediction Z",
    flag = "combat_silent_prediction_z",
    default = "",
    placeholder = "Prediction Z",
    tooltip = "Depth prediction multiplier for silent aim",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilentAimPredZ = num else getgenv().SilentAimPredZ = 0.115 end
    end,
})

silentSection:toggle({
    name = "Auto Prediction",
    flag = "combat_silent_auto_prediction",
    default = false,
    tooltip = "Enable auto prediction for silent aim",
    callback = function(val) getgenv().SilentAimAutoPrediction = val end,
})

silentSection:dropdown({
    name = "Auto Prediction Mode",
    flag = "combat_silent_auto_prediction_mode",
    items = { "Regular", "Ping based", "Advanced" },
    multi = false,
    default = "Regular",
    tooltip = "Select auto prediction mode for silent aim",
    callback = function(val) getgenv().SilentAimAutoPredictionMode = val end,
})

silentSection:toggle({
    name = "Anti Ground Shot",
    flag = "combat_silent_antiground",
    default = false,
    tooltip = "Prevent shooting at the ground",
    callback = function(val) getgenv().SilentAimAntiGround = val end,
})

silentSection:textbox({
    name = "Anti Curve",
    flag = "combat_silent_anticurve",
    default = "",
    placeholder = "Anti Curve",
    tooltip = "Prevent bullets from curving around corners",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilentAimAntiCurve = num else getgenv().SilentAimAntiCurve = 8 end
    end,
})

silentSection:toggle({
    name = "Wall Check",
    flag = "combat_silent_wall_check",
    default = false,
    tooltip = "Require line of sight to target",
    callback = function(val) getgenv().SilentAimWallCheck = val end,
})

silentSection:toggle({
    name = "Knocked Check",
    flag = "combat_silent_knocked_check",
    default = false,
    tooltip = "Ignore knocked players",
    callback = function(val) getgenv().SilentAimKnockCheck = val end,
})

silentSection:toggle({
    name = "Friend Check",
    flag = "combat_silent_friend_check",
    default = false,
    tooltip = "Ignore friends",
    callback = function(val) getgenv().SilentAimFriendCheck = val end,
})

silentSection:toggle({
    name = "Forcefield Check",
    flag = "combat_silent_forcefield_check",
    default = false,
    tooltip = "Ignore enemies with forcefield",
    callback = function(val) getgenv().SilentAimForcefieldCheck = val end,
})

silentSection:textbox({
    name = "Enemy Switching Delay",
    flag = "combat_silent_switching_delay",
    default = "",
    placeholder = "Enemy switching delay",
    tooltip = "Time before switching to a new target",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilentAimEnemySwitchDelay = num else getgenv().SilentAimEnemySwitchDelay = nil end
    end,
})

local espSection = visuals:section({ name = "ESP" })

espSection:toggle({
    name = "Enemy ESP",
    flag = "visuals_enemy_esp",
    default = false,
    tooltip = "Master switch for enemy ESP",
    callback = function(val) getgenv().EnemyESP = val end,
})

espSection:toggle({
    name = "Box 2D",
    flag = "visuals_box_2d",
    default = false,
    tooltip = "Draw a 2D box around players",
    callback = function(val) getgenv().Box2D = val end,
})

espSection:toggle({
    name = "Box 3D",
    flag = "visuals_box_3d",
    default = false,
    tooltip = "Draw a 3D box around players",
    callback = function(val) getgenv().Box3D = val end,
})

espSection:toggle({
    name = "Name",
    flag = "visuals_name",
    default = false,
    tooltip = "Show player names",
    callback = function(val) getgenv().NameESP = val end,
})

espSection:toggle({
    name = "Distance",
    flag = "visuals_distance",
    default = false,
    tooltip = "Show distance to players",
    callback = function(val) getgenv().DistanceESP = val end,
})

espSection:toggle({
    name = "Health Bar",
    flag = "visuals_healthbar",
    default = false,
    tooltip = "Show health bars above players",
    callback = function(val) getgenv().HealthBar = val end,
})

espSection:toggle({
    name = "Tracers",
    flag = "visuals_tracers",
    default = false,
    tooltip = "Draw lines from your position to players",
    callback = function(val) getgenv().Tracers = val end,
})

espSection:toggle({
    name = "Targets",
    flag = "visuals_targets",
    default = false,
    tooltip = "shows enemies name you are fighting in red",
    callback = function(val) getgenv().Targets = val end,
})

local worldSection = visuals:section({ name = "World", side = "right" })

worldSection:toggle({
    name = "Rain",
    flag = "visuals_rain",
    default = false,
    tooltip = "Enable rain effect",
    callback = function(val) getgenv().Rain = val end,
})

worldSection:toggle({
    name = "Snow",
    flag = "visuals_snow",
    default = false,
    tooltip = "Enable snow effect",
    callback = function(val) getgenv().Snow = val end,
})

worldSection:toggle({
    name = "Trails",
    flag = "visuals_trails",
    default = false,
    tooltip = "Show trails behind players",
    callback = function(val) getgenv().Trails = val end,
})

local gunSection = weapons:section({ name = "Gun Config" })

gunSection:toggle({
    name = "Enable",
    flag = "weapons_gun_config_enable",
    default = false,
    tooltip = "Enable per‑weapon hit chance settings",
    callback = function(val) getgenv().GunConfigEnabled = val end,
})

gunSection:textbox({
    name = "Double Barrel Hit Chance",
    flag = "weapons_hitchance_doublebarrel",
    default = "",
    placeholder = "Double Barrel HitChance",
    tooltip = "Hit chance for Double Barrel",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().DoubleBarrelHitChance = num else getgenv().DoubleBarrelHitChance = nil end
    end,
})

gunSection:textbox({
    name = "Revolver Hit Chance",
    flag = "weapons_hitchance_revolver",
    default = "",
    placeholder = "Revolver HitChance",
    tooltip = "Hit chance for Revolver",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().RevolverHitChance = num else getgenv().RevolverHitChance = nil end
    end,
})

gunSection:textbox({
    name = "Shotgun Hit Chance",
    flag = "weapons_hitchance_shotgun",
    default = "",
    placeholder = "Shotgun HitChance",
    tooltip = "Hit chance for Shotgun",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().ShotgunHitChance = num else getgenv().ShotgunHitChance = nil end
    end,
})

gunSection:textbox({
    name = "Tactical Shotgun Hit Chance",
    flag = "weapons_hitchance_tacticalshotgun",
    default = "",
    placeholder = "Tactical Shotgun HitChance",
    tooltip = "Hit chance for Tactical Shotgun",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().TacticalShotgunHitChance = num else getgenv().TacticalShotgunHitChance = nil end
    end,
})

gunSection:textbox({
    name = "USP Hit Chance",
    flag = "weapons_hitchance_usp",
    default = "",
    placeholder = "USP HitChance",
    tooltip = "Hit chance for USP",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().USPHitChance = num else getgenv().USPHitChance = nil end
    end,
})

gunSection:textbox({
    name = "Silencer Hit Chance",
    flag = "weapons_hitchance_silencer",
    default = "",
    placeholder = "Silencer HitChance",
    tooltip = "Hit chance for Silencer",
    callback = function(val)
        local num = tonumber(val)
        if num then getgenv().SilencerHitChance = num else getgenv().SilencerHitChance = nil end
    end,
})

local featureSection = weapons:section({ name = "Weapon Features", side = "right" })

featureSection:toggle({
    name = "Prefire",
    flag = "weapons_prefire",
    default = false,
    tooltip = "Enable prefire: automatically fire when enemy is about to peek",
    callback = function(val) getgenv().Prefire = val end,
})

featureSection:toggle({
    name = "Auto Shoot",
    flag = "weapons_autoshoot",
    default = false,
    tooltip = "Automatically shoot when locked onto a target",
    callback = function(val) getgenv().AutoShoot = val end,
})

featureSection:toggle({
    name = "Auto Reload",
    flag = "weapons_autoreload",
    default = false,
    tooltip = "Automatically reload when magazine is empty",
    callback = function(val) getgenv().AutoReload = val end,
})

featureSection:toggle({
    name = "No Spread",
    flag = "weapons_nospread",
    default = false,
    tooltip = "Remove bullet spread (makes shots perfectly accurate)",
    callback = function(val) getgenv().NoSpread = val end,
})

local panicSection = misc:section({ name = "Panic", side = "left" })

panicSection:toggle({
    name = "Panic Mode",
    flag = "misc_panic",
    default = false,
    tooltip = "Enable panic mode keybind",
    callback = function(val) getgenv().PanicMode = val end,
})

panicSection:keybind({
    name = "Panic Key",
    flag = "misc_panic_key",
    default = Enum.KeyCode.P,
    tooltip = "Key to trigger panic mode",
    callback = function(newKey)
        if newKey and type(newKey) == "userdata" and typeof(newKey) == "EnumItem" then
            getgenv().PanicKey = newKey
        else
            getgenv().PanicKey = Enum.KeyCode.P
        end
    end,
})

panicSection:dropdown({
    name = "Panic Action",
    flag = "misc_panic_action",
    items = { "Disable all", "Unload" },
    multi = false,
    default = "Disable all",
    tooltip = "Action to perform when panic key is pressed",
    callback = function(val) getgenv().PanicAction = val end,
})

local autoBuySection = misc:section({ name = "Auto Buy", side = "left" })

autoBuySection:toggle({
    name = "Auto Buy Armor",
    flag = "misc_autobuy_armor",
    default = false,
    tooltip = "Automatically buy armor when near a buy pad",
    callback = function(val) getgenv().AutoBuyArmor = val end,
})

autoBuySection:toggle({
    name = "Auto Buy Food",
    flag = "misc_autobuy_food",
    default = false,
    tooltip = "Automatically buy selected food items when near a buy pad",
    callback = function(val) getgenv().AutoBuyFood = val end,
}

autoBuySection:toggle({
    name = "Auto Eat",
    flag = "misc_autoeat",
    default = false,
    tooltip = "Auto eats food when health lowers",
    callback = function(val)
        getgenv().AutoEat = val
    end,
})

autoBuySection:toggle({
    name = "Auto Buy Gun",
    flag = "misc_autobuy_gun",
    default = false,
    tooltip = "Automatically buy selected weapons when near a buy pad",
    callback = function(state) getgenv().AutoBuygun = state
    end
})

autoBuySection:slider({
    name = "Buy Cooldown",
    flag = "misc_autobuy_cooldown",
    default = 5,
    min = 0,
    max = 30,
    interval = 0.5,
    suffix = "s",
    tooltip = "Time between buy attempts",
    callback = function(val) getgenv().AutoBuyCooldown = val end,
})

autoBuySection:slider({
    name = "Buy Proximity",
    flag = "misc_autobuy_proximity",
    default = 20,
    min = 0,
    max = 100,
    interval = 1,
    suffix = "studs",
    tooltip = "Max distance to a buyable item",
    callback = function(val) getgenv().AutoBuyProximity = val end,
})
local antiModSection = misc:section({ name = "Anti Mod", side = "right" })

antiModSection:toggle({
    name = "Anti Mod",
    flag = "misc_antimod",
    default = false,
    tooltip = "Enable anti‑mod detection",
    callback = function(val) getgenv().AntiModEnabled = val end,
})

antiModSection:toggle({
    name = "Check on Load",
    flag = "misc_antimod_checkonload",
    default = true,
    tooltip = "Check for staff when the script loads",
    callback = function(val) getgenv().CheckModsOnLoad = val end,
})

antiModSection:toggle({
    name = "Group Check",
    flag = "misc_antimod_groupcheck",
    default = false,
    tooltip = "Check if mods are in a specific group",
    callback = function(val) getgenv().GroupCheck = val end,
})

antiModSection:dropdown({
    name = "Mod Action",
    flag = "misc_antimod_action",
    items = { "Notify", "Crash", "Server hop", "Kick" },
    multi = false,
    default = "Notify",
    tooltip = "Action to take when a mod is detected",
    callback = function(val) getgenv().ModAction = val end,
})

local resolverSection = misc:section({ name = "Resolver", side = "right" })

resolverSection:toggle({
    name = "Resolver",
    flag = "misc_resolver_enabled",
    default = false,
    tooltip = "Enable resolver to fix anti‑aim",
    callback = function(val) getgenv().ResolverEnabled = val end,
})

resolverSection:toggle({
    name = "Auto Resolve",
    flag = "misc_resolver_auto",
    default = false,
    tooltip = "Automatically resolve enemy anti‑aim",
    callback = function(val) getgenv().AutoResolver = val end,
})

resolverSection:keybind({
    name = "Resolver Key",
    flag = "misc_resolver_key",
    default = Enum.KeyCode.R,
    tooltip = "Key to manually trigger resolver",
    callback = function(newKey)
        if newKey and type(newKey) == "userdata" and typeof(newKey) == "EnumItem" then
            getgenv().ResolverKey = newKey
        else
            getgenv().ResolverKey = Enum.KeyCode.R
        end
    end,
})

local config = settings:section({ name = "Theming System", side = "right" })
config:toggle({
    name = "Keybind List",
    flag = "settings_keybind_list",
    default = false,
    callback = function(bool) window.toggle_list(bool) end
})
config:toggle({
    name = "Player List",
    flag = "settings_player_list",
    default = false,
    callback = function(bool) window.toggle_playerlist(bool) end
})
config:toggle({
    name = "Watermark",
    flag = "settings_watermark",
    default = false,
    callback = function(bool) window.toggle_watermark(bool) end
})
config:keybind({
    name = "UI Bind",
    flag = "settings_ui_bind",
    default = Enum.KeyCode.End,
    display = "menu",
    callback = window.set_menu_visibility
})
config:slider({
    name = "Colorpicker Animation Speed",
    flag = "settings_color_picker_anim_speed",
    min = 0,
    max = 5,
    default = 2,
    interval = 0.01,
    suffix = ""
})
config:colorpicker({
    color = Color3.fromHex("#6464FF"),
    flag = "settings_accent",
    callback = function(color) library:update_theme("accent", color) end
})
config:button({ name = "Copy JobId", callback = function() setclipboard(game.JobId) end })
config:button({ name = "Copy GameID", callback = function() setclipboard(game.GameId) end })
config:button({ name = "Copy Join Script", callback = function()
    setclipboard('game:GetService("TeleportService"):TeleportToPlaceInstance(' .. game.PlaceId .. ', "' .. game.JobId .. '", game.Players.LocalPlayer)')
end })
config:button({ name = "Rejoin", callback = function()
    game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
end })

local configs_section = settings:section({ name = "Configuration System", side = "left" })
local dir = library.directory .. "/configs/"
library.config_holder = configs_section:dropdown({
    name = "Configs",
    flag = "settings_config_name_list",
    items = {}
})
configs_section:textbox({ flag = "settings_config_name_text_box" })
configs_section:button({ name = "Create", callback = function()
    writefile(dir .. flags["settings_config_name_text_box"] .. ".cfg", library:get_config())
    library:config_list_update()
end })
configs_section:button({ name = "Delete", callback = function()
    library:panel({
        name = "Are you sure you want to delete " .. flags["settings_config_name_list"] .. " ?",
        options = { "Yes", "No" },
        callback = function(option)
            if option == "Yes" then
                delfile(dir .. flags["settings_config_name_list"] .. ".cfg")
                library:config_list_update()
            end
        end,
    })
end })
configs_section:button({ name = "Load", callback = function()
    library:load_config(readfile(dir .. flags["settings_config_name_list"] .. ".cfg"))
end })
configs_section:button({ name = "Save", callback = function()
    writefile(dir .. flags["settings_config_name_text_box"] .. ".cfg", library:get_config())
    library:config_list_update()
end })
configs_section:button({ name = "Unload Config", callback = function() library:load_config(old_config) end })
configs_section:button({ name = "Unload Menu", callback = function() library:unload() end })

library:config_list_update()
combat.open_tab()
library:notification({ text = "Astra UI loaded." })

return library
]])()
